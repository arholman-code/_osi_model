This is a basic visual representation of the transfer of packets from one router to the next.  On display (eventually), will be
a packet being passed from level to level and encoded.  It will be converted to binary, a sine wave, and utilize ARQ stop-and-wait
as well as a simple CRC check.